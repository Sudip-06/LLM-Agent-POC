services:
  - type: web
    name: llm-agent-poc
    env: node
    plan: free
    region: oregon
    buildCommand: "npm ci"
    startCommand: "npm start"
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NODE_ENV
        value: production
      # Add your secrets in Render Dashboard or here (not recommended to commit secrets)
      # - key: GROQ_API_KEY
      #   sync: false
      # - key: GEMINI_API_KEY
      #   sync: false
      # - key: GOOGLE_API_KEY
      #   sync: false
      # - key: GOOGLE_CSE_ID
      #   sync: false
      # - key: AIPIPE_URL
      #   sync: false
      # - key: AIPIPE_AUTH
      #   sync: false
